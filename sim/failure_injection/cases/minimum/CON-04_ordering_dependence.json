{
  "case_id": "CON-04",
  "suite": "CON",
  "description": "Ordering dependence test: same set of actions applied in different order must yield same outcome.",
  "seed_policy": { "inherit": true },
  "initial_state_overrides": {
    "N_users": 10,
    "initial_vault_balance_per_user": 1000,
    "initial_pocket_balance_per_user": 100,
    "initial_buffer_reserve": 1000,
    "initial_epoch": 0,
    "DSM_active": false,
    "DSM_expires_at_epoch": null
  },
  "parameter_overrides": {
    "EAF_cap_per_user_per_epoch": 50,
    "vault_to_pocket_slope_cap_per_user_per_epoch": 40,
    "DSM_duration_epochs": 3,
    "buffer_reserve_floor_ratio": 0.15
  },
  "epoch_actions": [
    { "type": "REQUEST_ESSENTIAL_SPEND", "epoch": 1, "user_selector": "all", "amount": 60 },
    { "type": "REQUEST_VAULT_TO_POCKET", "epoch": 1, "user_selector": "pct:50", "amount": 100 },
    { "type": "REQUEST_ESSENTIAL_SPEND", "epoch": 1, "user_selector": "pct:50", "amount": 10 },
    { "type": "ADVANCE_EPOCH", "to_epoch": 2 }
  ],
  "expected_properties": ["REPLAY_OK", "STATE_MACHINE_VALID", "EAF_CAP_ENFORCED", "SLOPE_CAP_ENFORCED"],
  "expected_classification": {
    "first_failure_tier": 0,
    "first_failure_type": "NONE",
    "note": "Implementation should canonicalize ordering or ensure commutativity where required."
  },
  "notes": "Runner should execute an internal permuted replay variant for this case."
}
