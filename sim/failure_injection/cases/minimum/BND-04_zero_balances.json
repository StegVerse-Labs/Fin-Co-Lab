{
  "case_id": "BND-04",
  "suite": "BND",
  "description": "All users start with zero vault and zero pocket; ensure deterministic denials and no underflow.",
  "seed_policy": { "inherit": true },
  "initial_state_overrides": {
    "N_users": 100,
    "initial_vault_balance_per_user": 0,
    "initial_pocket_balance_per_user": 0,
    "initial_buffer_reserve": 50000,
    "initial_epoch": 0,
    "DSM_active": false,
    "DSM_expires_at_epoch": null
  },
  "parameter_overrides": {
    "EAF_cap_per_user_per_epoch": 100,
    "vault_to_pocket_slope_cap_per_user_per_epoch": 250,
    "DSM_duration_epochs": 3,
    "buffer_reserve_floor_ratio": 0.15
  },
  "epoch_actions": [
    { "type": "REQUEST_ESSENTIAL_SPEND", "epoch": 1, "user_selector": "all", "amount": 20 },
    { "type": "REQUEST_VAULT_TO_POCKET", "epoch": 1, "user_selector": "pct:40", "amount": 1000 },
    { "type": "ADVANCE_EPOCH", "to_epoch": 2 }
  ],
  "expected_properties": ["NO_NEGATIVE_BALANCES", "REPLAY_OK", "STATE_MACHINE_VALID"],
  "expected_classification": { "first_failure_tier": 0, "first_failure_type": "NONE", "note": "Insufficient funds -> deterministic denials." },
  "notes": "Verifies underflow resistance and denial consistency."
}
