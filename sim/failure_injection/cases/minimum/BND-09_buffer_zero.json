{
  "case_id": "BND-09",
  "suite": "BND",
  "description": "Buffer reserve = 0. Ensure no negative buffer; define deterministic handling of buffer draw attempts.",
  "seed_policy": { "inherit": true },
  "initial_state_overrides": {
    "N_users": 100,
    "initial_vault_balance_per_user": 10000,
    "initial_pocket_balance_per_user": 500,
    "initial_buffer_reserve": 0,
    "initial_epoch": 0,
    "DSM_active": false,
    "DSM_expires_at_epoch": null
  },
  "parameter_overrides": {
    "EAF_cap_per_user_per_epoch": 100,
    "vault_to_pocket_slope_cap_per_user_per_epoch": 250,
    "DSM_duration_epochs": 3,
    "buffer_reserve_floor_ratio": 0.15
  },
  "epoch_actions": [
    { "type": "REQUEST_ESSENTIAL_SPEND", "epoch": 1, "user_selector": "all", "amount": 80 },
    { "type": "ADVANCE_EPOCH", "to_epoch": 2 }
  ],
  "expected_properties": ["NO_NEGATIVE_BALANCES", "REPLAY_OK", "STATE_MACHINE_VALID", "EAF_CAP_ENFORCED"],
  "expected_classification": {
    "first_failure_tier": 0,
    "first_failure_type": "NONE",
    "note": "If buffer is 0, system must not underflow; any buffer draw must be denied or handled deterministically."
  },
  "notes": "Detects buffer underflow and negative reserve bugs."
}
